---
name: source-{connector_name}
description: {ConnectorName}ソースのスキーマ取得・型変換・接続確認を実行する
allowed-tools: Bash, Read, Write, Glob, Grep
---

# {ConnectorName} ソースSkill

オーケストレーター (`setup-pipeline.md`) から Read で読み込まれ、{ConnectorName} ソース固有の処理を実行する。

## 必要環境変数

| 変数 | モードA | モードB | 説明 |
|------|---------|---------|------|
| `{PREFIX}_CONNECTION_ID` | 必須 | 不要 | 既存TROCCO接続ID |
| `{PREFIX}_...` | 不要 | 必須 | 認証情報（モードBで必要な変数を列挙） |

**モード判定:**
- `{PREFIX}_CONNECTION_ID` が設定済み → **モードA**（既存接続参照、推奨）
- `{PREFIX}_CONNECTION_ID` が空 → **モードB**（Terraformで新規作成）

## Step 1: スキーマ取得

データソースのAPIまたはユーザー入力でフィールド/カラム定義を取得する。

```bash
set -a && source .env.local && set +a
# ソース固有のAPI呼び出しでスキーマ情報を取得
# 結果を /tmp/{connector}-fields-response.json に保存
```

### HTTPステータス確認

- 200: 正常 → Step 2 へ
- 401/403: 認証エラー → 認証情報の確認を案内して停止
- 404: リソース不存在 → ID/URLの確認を案内して停止
- その他: エラー内容を表示して停止

## Step 2: 型変換

**reference/type-mapping.md** の該当セクションに従い、フィールド情報を変換する。

1. `input_option_columns` 配列を生成（フィールド名 + TROCCOカラムタイプ）
2. `filter_columns` 配列を生成（英語カラム名 + src + タイプ + format）

日本語フィールド名の英語変換はClaude Codeの推論で実行。

## Step 3 (src): 接続確認

### モードA: 既存接続ID参照

`{PREFIX}_CONNECTION_ID` が設定済み → そのIDを使用。API呼び出しスキップ。

### モードB: Terraform新規作成

`{PREFIX}_CONNECTION_ID` が空 → TROCCO API接続一覧を試行:

```bash
set -a && source .env.local && set +a
if [ -n "${PREFIX}_CONNECTION_ID" ]; then
  echo "{PREFIX}_CONNECTION_ID=${!PREFIX_CONNECTION_ID} (設定済み → このIDを使用)"
else
  RESULT=$(curl -s -w "\nHTTP_STATUS:%{http_code}" \
    "https://trocco.io/api/connections/{connector_name}" \
    -H "Authorization: Token ${TROCCO_API_KEY}")
  HTTP_CODE=$(echo "$RESULT" | tail -1 | sed 's/HTTP_STATUS://')
  BODY=$(echo "$RESULT" | sed '$d')

  if [ "$HTTP_CODE" = "200" ]; then
    echo "$BODY" | jq '.items[] | {id, name}' 2>/dev/null || echo "既存接続なし"
  elif [ "$HTTP_CODE" = "401" ] || [ "$HTTP_CODE" = "403" ]; then
    echo "API HTTP ${HTTP_CODE}: 接続一覧APIへのアクセス権限がありません"
    echo "→ TROCCO管理画面 > 接続情報 でIDを確認し .env.local に設定してください"
  else
    echo "API HTTP ${HTTP_CODE}: ${BODY}"
  fi
fi
```

## HCL情報

### input_option 構造

```
input_option_type = "{connector_name}"
input_option = {
  {connector_name}_input_option = {
    {connector_name}_connection_id = local.{connector_name}_connection_id
    # ソース固有のオプション
    input_option_columns = var.input_columns
  }
}
```

### connection resource (モードB時)

```hcl
resource "trocco_connection" "{connector_name}_source" {
  count = var.{prefix}_connection_id == null ? 1 : 0

  name            = var.{prefix}_connection_name
  connection_type = "{connector_name}"
  description     = "{ConnectorName} - auto-generated by TROCCO Pipeline Builder"

  # ソース固有の認証フィールド
}

locals {
  {connector_name}_connection_id = (
    var.{prefix}_connection_id != null
    ? var.{prefix}_connection_id
    : trocco_connection.{connector_name}_source[0].id
  )
}
```

### variables.tf ({ConnectorName}固有)

```hcl
variable "{prefix}_connection_id" {
  description = "既存TROCCO {ConnectorName}接続ID（設定時は接続作成をスキップ）"
  type        = number
  default     = null
}

variable "{prefix}_connection_name" {
  description = "TROCCO上の{ConnectorName}接続名（新規作成時のみ使用）"
  type        = string
  default     = "{connector_name}-auto"
}

# ソース固有の変数を追加
```

### TF_VAR export パターン

```bash
# Source: {ConnectorName}
[ -n "${PREFIX}_CONNECTION_ID" ] && export TF_VAR_{prefix}_connection_id="${PREFIX}_CONNECTION_ID"
[ -z "${PREFIX}_CONNECTION_ID" ] && export TF_VAR_{prefix}_...="..."  # 認証情報
```

---

## 新規ソース追加チェックリスト

1. このテンプレートをコピーして `.claude/skills/sources/{connector_name}/SKILL.md` を作成
2. プレースホルダを置換:
   - `{connector_name}` → ソース正規化名（例: `mysql`, `postgresql`）
   - `{ConnectorName}` → 表示名（例: `MySQL`, `PostgreSQL`）
   - `{PREFIX}` → 環境変数プレフィックス（例: `MYSQL`, `PG`）
   - `{prefix}` → Terraform変数プレフィックス（例: `mysql`, `pg`）
3. `reference/connector-catalog.md` にエントリ追加
4. `.claude/skills/infrastructure/generate-env/env-vars.json` の `sources` セクションに変数定義追加
5. （任意）`reference/sources/{connector_name}.md` に詳細リファレンス作成
6. （任意）`reference/type-mapping.md` に型変換ルール追加
